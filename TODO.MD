1.  接口不要设计成 模板或者类型返回值类型 
    比如 接口这么写 void xxxx(const std::string& sInput,std::u16string& sOutput)
    内存只分配一次，没有传参构造和 内存拷贝
2. 复用，如果能保证 一个变量只被一个线程使用，那么可以使用类共享 定义。而非局部，减少频繁构造
3、减少多线程影响，真要快 现代化CPU都是多核心的，多线程是一定要考虑的
4、尽量避免内存操作。一份内存走到黑。多用std::move

直接函数局部持有所有资源